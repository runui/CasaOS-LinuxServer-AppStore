name: linuxserver-ferdium
services:
  app:
    image: linuxserver/ferdium:7.2.3
    restart: unless-stopped
    environment:
      PUID: $PUID
      PGID: $PGID
      TZ: $TZ
    ports:
      - target: 3000
        published: 3000
        protocol: tcp
      - target: 3001
        published: 3001
        protocol: tcp
    volumes:
      - type: bind
        source: /DATA/AppData/ferdium/config
        target: /config
x-casaos:
  main: app
  icon: https://cdn.jsdelivr.net/gh/runui/CasaOS-LinuxServer-AppStore@gh-pages/apps/wisdomsky.linuxserver.linuxserverferdium/assets/icon.png
  title:
    en_US: Ferdium (LinuxServer.io)
  id: wisdomsky.linuxserver.linuxserverferdium
  version: "7.2.2"
  port_map: '3000'
