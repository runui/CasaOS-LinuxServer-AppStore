name: linuxserver-pwndrop
services:
  app:
    image: linuxserver/pwndrop:1.0.1
    restart: unless-stopped
    environment:
      PUID: $PUID
      PGID: $PGID
      TZ: $TZ
      SECRET_PATH: ''
    ports:
      - target: 8080
        published: 8080
        protocol: tcp
    volumes:
      - type: bind
        source: /DATA/AppData/pwndrop/config
        target: /config
x-casaos:
  main: app
  icon: https://cdn.jsdelivr.net/gh/runui/CasaOS-LinuxServer-AppStore@gh-pages/apps/wisdomsky.linuxserver.linuxserverpwndrop/assets/icon.png
  title:
    en_US: Pwndrop (LinuxServer.io)
  id: wisdomsky.linuxserver.linuxserverpwndrop
  version: "1.0.1"
  port_map: '8080'
